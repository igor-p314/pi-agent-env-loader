name: Publish to npm

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    if: github.event.repository.default_branch == 'main' || github.event.repository.default_branch == 'master'
    permissions:
      contents: read
      id-token: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22.x'
          registry-url: 'https://registry.npmjs.org'

      - name: Install dependencies
        run: npm ci

      - name: Build package
        run: npm run build

      - name: Run tests
        run: npm test

      - name: Check for version in npm
        run: |
          VERSION=$(node -p "require('./package.json').version")
          if npm view @htwdev/pi-env-loader@$VERSION version &>/dev/null; then
            echo "Version $VERSION already published"
            exit 1
          fi

      - name: Publish to npm
        run: npm publish --provenance --access public